El endpoint no encontró picks porque está usando fecha UTC.

Quiero que el filtro de "hoy" NO use toISOString().

En su lugar:
- usa created_at::date = CURRENT_DATE (lado DB)
- o convierte correctamente a timezone local antes de comparar

No cambies nada más.
Solo corrige el filtro de fecha para que detecte correctamente picks de hoy.